{
  "description": "Fallow quality baseline. Counters may shrink but not grow; averageMaintainability may rise but not drop. Regenerate with `npm run check:quality -- --update` (commit the diff in the same PR that moves the metric). 2026-07-17 (side-panels follow-ups): cloneGroups 40→39, duplicatedLines 999→970 — ProjectionObserverSet now owns the snapshot supplier, so the composer/transcript/chrome projections' subscribe/emit delegation collapsed below the clone threshold, and the shared applyTitleGenerationResult helper deduped the user-rename-wins policy out of InputController + sidePanelCallbacks.",
  "fallowVersion": "2.91.0",
  "metrics": {
    "deadCodeIssues": 0,
    "circularDependencies": 0,
    "reExportCycles": 0,
    "boundaryViolations": 0,
    "cloneGroups": 39,
    "duplicatedLines": 970,
    "complexFunctions": 244,
    "criticalComplexity": 5,
    "averageMaintainability": 90.2
  }
}
