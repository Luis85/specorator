// Task 3 fleshes this out into the real Vue->engine transcript seam (mirrors
// ChatShellCallbacks). Stubbed now so transcriptKeys.ts has a concrete type to
// inject/provide against.
// eslint-disable-next-line @typescript-eslint/no-empty-object-type -- intentional placeholder, filled in by Task 3
export interface TranscriptCallbacks {}
