/**
 * Toolbar domain DTO barrel (P6, SPEC-TC-003/006). One-stop import for the
 * descriptor DTOs + the per-tab control bag.
 */
export type {
	ModelOption,
	ModeDescriptor,
	ReasoningDescriptor,
	ServiceTierDescriptor,
	ToolbarCatalog,
} from './ToolbarCatalog';
export type { TabControls } from './TabControls';
