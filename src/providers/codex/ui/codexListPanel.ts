/**
 * Codex re-export of the shared vault list-panel scaffold. The panel chrome is
 * provider-neutral and shared with Opencode under `shared/settings/`; this
 * module keeps the historical Codex import path stable.
 */
export { renderVaultListPanel as renderCodexListPanel } from '../../../shared/settings/vaultAgentListPanel';
