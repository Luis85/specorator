---
title: Session handoff — 2026-06-04 (Q-1 complete)
date: 2026-06-04
status: open
scope: pickup of remaining 2026-06-02 plan items after Q-1 closure
related:
  - "[[2026-06-02-codebase-review-and-improvement-plan]]"
  - "[[2026-06-03-plan-execution]]"
  - "[[translate-validator-helper-strings]]"
  - "[[0001-transport-agnostic-provider-seam]]"
---

# Session handoff — 2026-06-04 (Q-1 complete)

Picks up from the 2026-06-03 handoff. Q-1 (Notice i18n sweep) is now fully
closed including the ESLint rule and post-review fixes. Working tree on `main`,
user opted to commit directly without a feature branch.

## What shipped this session (17 commits, all on main)

| Commit | Phase / item | Verified |
|--------|--------------|----------|
| `d583585` | Q-1 chunk 4 — OpencodeAgentSettings (7/14 routed, new `provider.opencode.subagent.*`) | tc/lint/test/build |
| `4d68527` | Q-1 chunk 5 — AgentSettings claude (1/11 routed, extends `settings.subagents.*`) | tc/lint/test/build |
| `e7afc33` | Q-1 chunk 6 — tabControllers (3/11 routed, new `chat.fork.unsupportedProvider`, `chat.input.chatServiceInitFailed`) | tc/lint/test/build |
| `82aa187` | Q-1 chunk 7 — SlashCommandSettings (9/10 routed, extends `settings.slashCommands.*`) | tc/lint/test/build |
| `5a224e9` | Q-1 docs — subspace policy codified + validator-helper issue tracked | tc/lint/test/build |
| `a7a77dc` | Q-1 chunk 8 — CodexSubagentSettings (7/9 routed, new `provider.codex.subagent.*`) | tc/lint/test/build |
| `96e30e4` | Q-1 chunk 9 — main.ts (9/9 routed, new `chat.context.*` + new top-level `diagnostics.*`) | tc/lint/test/build |
| `417d6db` | Q-1 chunk 10 — AgentBoardView (9/9 routed, new top-level `tasks.*` namespace) | tc/lint/test/build |
| `0e53e19` | Q-1 chunk 11 — CodexSkillSettings (5/6 routed, new `provider.codex.skill.*`) | tc/lint/test/build |
| `a870d57` | Q-1 chunk 12 — EnvSnippetManager (5/6 routed) | tc/lint/test/build |
| `d1d2b1e` | Q-1 chunk 13 — chat-misc sweep (13 sites across 5 files) | tc/lint/test/build |
| `6c69d4a` | Q-1 chunk 14 — tasks-misc sweep (15 sites across 6 files) | tc/lint/test/build |
| `e987f03` | Q-1 chunk 15 — settings + app-commands sweep (18 sites across 8 files) | tc/lint/test/build |
| `eac33ff` | Q-1 chunk 16 — providers + inline-edit + bang-bash sweep (15 sites across 6 files) | tc/lint/test/build |
| `ad1d5ce` | Q-1 final — ESLint rule blocking hardcoded `new Notice()` | tc/lint/test/build |
| `adfb6d8` | Q-1 review fixes — subspace cohesion + orphan whitespace | tc/lint/test/build |

Every commit verified with `npx tsc --noEmit && npm run lint && npm run test`
before pushing. Current state: 6623 tests pass / 35 skipped / 356 suites, lint
clean (with the new no-hardcoded-Notice rule active), build pass.

Out-of-range commit in the same window: `88ea808 docs(convention): promote
canonical notes from .context/ to docs/` (user-authored, scoped `.context/` to
throwaway-only and created `docs/handoffs/`).

## Q-1 closure — what landed

- **17 commits across chunks 1–16** plus the ESLint rule plus the review fix.
- **~155 sites routed** across ~30 files. Chunks 1–3 ran in the prior 2026-06-03
  session; chunks 4–16 + ESLint rule + review fix ran in this session.
- **Zero hardcoded `new Notice('...')` / ``new Notice(`...`)``** in `src/`.
  Verifiable with `grep -rn "new Notice(['\"\`]" src/ | wc -l` → 0.
- **~25 identifier pass-throughs remain** — all carry English from helper
  functions (`validate*`, `parseOptional*`, `runToolbarAction`,
  `notifyImageError`, `for (const warning of warnings) new Notice(warning)`).
  These are tracked at `docs/issues/translate-validator-helper-strings.md`.
- **Subspace policy codified** in
  `docs/reviews/2026-06-02-codebase-review-and-improvement-plan.md`:
  `chat.<feature>.*`, `settings.<feature>.*`, `provider.<id>.<feature>.*` with
  `settings.subagents.*` grandfathered.
- **ESLint rule live** at `eslint.config.mjs:103-128`. Blocks
  `NewExpression[callee.name="Notice"]` with `Literal` or `TemplateLiteral`
  first argument. Identifier pass-throughs and `new Notice(t(...))` allowed.
  Known minor gaps (aliased imports, parenthesized callees) documented in the
  chunk-16 review.

## Deploy state

TestVault build copy is current per `adfb6d8`. To redeploy:

```bash
OBSIDIAN_VAULT="D:/TestVault" npm run build
```

## What's NOT done — pick-up queue (linear-order)

### Validator-helper translation (Q-1 follow-up)

Tracked at `docs/issues/translate-validator-helper-strings.md`. Touches the
~25 pass-through sites left after Q-1. Requires a contract change on each
helper:

- `validateOpencodeAgentName`, `validateAgentName`, `validateCommandName`,
  `validateCodexSubagentName`, `validateCodexNicknameCandidates` — return
  `{ key: TranslationKey, params?: Record<string, string | number> } | null`
  instead of `string | null`.
- `parseOptionalNumber`, `parseOptionalPositiveInteger`,
  `parseOptionalJsonObjectOfBooleans`, `parseOptionalJson`,
  `parseOptionalJsonObject` — same change to the `error` field; the `label`
  parameter also needs to become a `TranslationKey` not a raw English string.
- `runToolbarAction({ failureMessage })` and `notifyImageError(message)` —
  parameter-passing pattern; change to `{ key, params }` shape.
- `taskCommands.ts:289` `for (const warning of warnings)` —
  `warnings[]` populated by `resolveProviderModel` / `buildTemplateVars`,
  needs upstream contract change.

Approximate scope: 3 contract changes across ~5 helper files, ~12 call sites
updated, ~25 new translation keys added under existing provider/feature
subspaces. Tests for the helpers will need parallel updates.

### Phase 1c F2 + F3 (PERF-4 tuning) — needs production measurement

| Item | Block |
|------|-------|
| F3 | Requires a one-off measurement session on a real ≥1000-message transcript with cold/warm OS cache. Mocked-fs Jest overstates the yield overhead by ~75×. Capture results in `docs/research/` (or `.context/` if not durable). |
| F2 | Tune `YIELD_EVERY_PARSED_LINES` (100) + `YIELD_EVERY_MERGED_ENTRIES` (50) against F3 numbers. Update inline comments next to each constant with the measured per-batch cost and chosen N rationale. |

### Q-4 — unit tests for security paths — closed 2026-06-04

All four targets now covered. Landing order:

| Path | Source LOC | Test LOC | Tests | Commit |
|------|-----------|----------|-------|--------|
| `HomeFileAdapter` | 75 | 194 | 27 | `ea57c69` |
| `ClaudeApprovalHandler` | 149 | 428 | ~30 | `d952a11` |
| `AcpToolStreamAdapter` | 132 | 346 | 18 | `846ab18` |
| `ClaudeRewindService` | 220 | 345 | 15 | `a353c1b` |

`HomeFileAdapter` pattern: real tmpdir via `mkdtempSync` / `rmSync` per test,
exercising the real `fs.promises` contract end-to-end. Cancellation skipped
with inline comment — no `AbortSignal` in the surface. Verify suite stayed
green at each landing.

### Q-7 — finish settings registry port

5 tabs still imperative: `general`, `claude`, `codex`, `opencode`, `cursor`
(~53 fields outstanding). Tracked at
`docs/issues/settings-registry-port-followup.md`. Delete the legacy fallback
renderers once all tabs are registry-driven.

### Q-NEW-2 — provider test parity

Cursor + Opencode lack approval-handler and MCP-dispatch coverage. Claude has
21 suites, Codex 17, Opencode 8, Cursor 6, ACP 5. Add ~2 suites per gap.

### ADR-0001 Phase 2b — RuntimeHost migration

Deferred for a focused PR. ~500 LOC mechanical refactor across 4 provider
runtimes + `tabControllers` + 5 test files. Type already defined at
`src/core/runtime/RuntimeHost.ts` with the cancel-dismiss invariant
documented inline. The migration replaces the seven `set*Callback` setters
with the typed host on each runtime.

### ADR-0001 Phase 3 — `core/transport/` extraction

Blocked on `docs/superpowers/plans/2026-05-30-cursor-integration-hardening.md`
PR2 (T5/T6/T8/T13/T14/T22/T25/T26/T27 still open). Phase 3 must land after
PR2 to avoid the documented file collision on `CursorChatRuntime.query`, ACP
subprocess kill, and ACP transport pending-request cleanup.

### Phase 2b ARCH-5 — split `InputController.ts`

1464 LOC after the `QueuedMessageController` extraction. Three seams remain:
input wiring, mention dispatch, steering state machine. Plan recommends
pairing with the RuntimeHost work above to amortize the test churn.

## How to resume

1. Open repo at `D:\Projects\claudian`. On `main` branch.
2. Check `git log --oneline -10` — top commit should be `adfb6d8`.
3. Read this file plus the plan doc
   (`docs/reviews/2026-06-02-codebase-review-and-improvement-plan.md`) and
   the validator-helper issue
   (`docs/issues/translate-validator-helper-strings.md`) to confirm state.
4. For the validator-helper sweep: the issue file at
   `docs/issues/translate-validator-helper-strings.md` already documents the
   proposed `{ key, params } | null` contract. Land in two phases: (a)
   migrate the `validate*` helpers (return contract change + call sites +
   test updates), (b) migrate the `parseOptional*` helpers + the
   `runToolbarAction` / `notifyImageError` parameter pattern.
5. For Phase 1c F3: spin up a real vault with a ≥1000-message Claude
   transcript. Capture `loadMs` from `tests/perf/conversationLoad.perf.test.ts`
   running against the real fs (or write a one-shot bench script under
   `dev/`). Compare cold-cache vs warm-cache. Record in
   `docs/research/2026-06-04-perf4-prod-measurement.md` (new).
6. For Q-4: target one path at a time. Start with `HomeFileAdapter` (smallest
   at 75 LOC) to land the testing pattern, then scale up.

## Helper scripts (gitignored under `.context/`)

Used during chunks 1–16 to inject English placeholders into the 9 non-en
locales. Idempotent (Python `setdefault`). Per the 2026-06-03 docs convention
they remain in `.context/` (throwaway-only scope):

- `add-chat-input-locale.sh` (chunk 1)
- `add-conv-controller-keys.sh` (chunk 2)
- `add-mcp-keys.sh` (chunk 3)
- `add-opencode-agent-keys.sh` (chunk 4)
- `add-claude-agent-load-key.sh` (chunk 5)
- `add-tab-controllers-keys.sh` (chunk 6)
- `add-slash-command-keys.sh` (chunk 7)
- `add-codex-subagent-keys.sh` (chunk 8)
- `add-main-context-keys.sh` (chunk 9)
- `add-tasks-board-keys.sh` (chunk 10)
- `add-codex-skill-keys.sh` (chunk 11)
- `add-env-snippet-keys.sh` (chunk 12)
- `add-chat-misc-keys.sh` (chunk 13)
- `add-tasks-misc-keys.sh` (chunk 14)
- `add-settings-misc-keys.sh` (chunk 15)
- `add-providers-misc-keys.sh` (chunk 16)

Future chunks should follow the same pattern: a one-off Python `setdefault`
script per key block, gitignored, idempotent.

## CLAUDE.md / memory state

Nothing changed in `~/.claude/projects/D--Projects-claudian/memory/MEMORY.md`
this session. Existing entries (window-timer convention, lint-clean policy,
release process, dev-build setup) remain accurate.

## One last context note

The user runs the project in caveman mode. All commit messages and code stay
in normal prose — caveman applies to chat replies only. Plan docs and handoff
notes likewise stay normal-prose so they remain readable in any mode.
